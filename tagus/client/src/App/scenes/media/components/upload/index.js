import React, { Component } from 'react';

class UploadImage extends Component {
    render() {
        return (
            <form>
                <input type="file" />
            </form>
        )
    }
}

export default UploadImage;